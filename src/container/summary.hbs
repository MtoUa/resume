<main class="row g-3 row-cols-2">
    {{#with main}}
        <section class="col">
            {{#with summary}}
                <div class="card">
                    <h2 class="card-header">{{title}}</h2>
                    <div class="card-body">
                        <p class="card-text">
                            {{text}}
                        </p>
                    </div>
                </div>
            {{/with}}
        </section>

        <section class="col">
            {{#with experience}}
                <div class="card">
                    <h3 class="card-header">{{title}}</h3>
                    <div class="card-body">
                        <p class="card-text">
                            {{text}}
                        </p>
                    </div>
                </div>
            {{/with}}
        </section>
    {{/with}}
</main>