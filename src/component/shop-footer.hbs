<hr class="my-4" />

<footer class="row mb-5">
    <nav class="nav">
        {{#each footer}}
            <div class="col-2">
                {{#each this}}
                    <a class="nav-link
            {{#if @last}}disabled{{/if}}" href="{{link}}">{{text}}</a>
                {{/each}}
            </div>
        {{/each}}
    </nav>
</footer>